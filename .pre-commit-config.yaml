repos:
  - repo: local
    hooks:
      - id: mttt-normalize
        name: MTTT normalize canonical fixtures
        entry: python -m mttt.cli normalize --data-dir fixtures/valid_minimal
        language: system
        pass_filenames: false

      - id: assert-no-diff
        name: Assert no git diff after normalization
        entry: git diff --exit-code
        language: system
        pass_filenames: false
